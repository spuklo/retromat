import java.text.SimpleDateFormat

allprojects {
    description 'retromat'
    group 'io.github.spuklo'
    version new SimpleDateFormat("yyyyMMdd-HHmmss").format(new Date(System.currentTimeMillis()))

    defaultTasks 'clean', 'build'
}
